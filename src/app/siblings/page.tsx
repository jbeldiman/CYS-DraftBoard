export default function SiblingsPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Siblings</h1>
      <p>Placeholder page — we’ll load sibling pairs + draft cost here.</p>
    </main>
  );
}